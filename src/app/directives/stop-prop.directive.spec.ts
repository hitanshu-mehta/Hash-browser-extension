import { StopPropDirective } from './stop-prop.directive';

describe('StopPropDirective', () => {
  it('should create an instance', () => {
    const directive = new StopPropDirective();
    expect(directive).toBeTruthy();
  });
});
